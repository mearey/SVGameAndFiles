/// @description Insert description here
// You can write your code in this editor
if (instance_exists(SettingsObj) and text != "Apply") {
	active = false
	sprite_index = MainMenuButton
} else {
	active = true	
}